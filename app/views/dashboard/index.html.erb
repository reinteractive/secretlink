<div class="container">
  <div class="header">
    <h1>All Secrets</h1>
    <%= link_to 'Create New', new_secret_path, class: 'btn' %>
  </div>

  <% @secrets.each do |secret| %>
    <%= render 'secret_item', secret: secret %>
  <% end %>
</div>
