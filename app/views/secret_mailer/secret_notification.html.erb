Hello <%= @secret.to_email %><br />
<br />
<%= @secret.from_email %> has shared a secret with you via topsekrit.<br />
<br />
<% if @secret.title.present? %>
  Title: <%= @secret.title %><br />
<% end %>
<% if @secret.comments.present? %>
  Comments: <%= @secret.comments %><br />
<% end %>
This link will show you the secret:<br />
<%= link_to "#{@request_host}/#{@secret.uuid}/#{@secret.secret_key}/#{@auth_token.hashed_token}", "#{@request_host}/#{@secret.uuid}/#{@secret.secret_key}/#{@auth_token.hashed_token}" %><br />
<br />
It can only be viewed once and will then be removed from the database.<br />
<% if @secret.expire_at.present? %>
  It will also expire and be removed if not accessed before <%= @secret.expire_at.strftime('%d %B %Y') %>.<br />
<% end %>
