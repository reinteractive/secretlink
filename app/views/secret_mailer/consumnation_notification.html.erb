Hello <%= @secret.from_email %>

This email is to notify you that <%= @secret.to_email %> viewed a secret that you shared with them via snapsecret.

<% if !@secret.title.blank? %>
  The title of the viewed secret was: <%= @secret.title %>
<% elsif !@secret.comments.blank? %>
  The comments on the viewed secret were: <%= @secret.title %>
<% end %>

This secret was created by you <%= time_ago_in_words(@secret.created_at) %> ago.

The encrypted information has now been deleted from the database.